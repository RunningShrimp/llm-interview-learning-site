# DECISIONS.md — 主 Agent 自主决策记录（无人值守模式，不等待用户确认）

| # | 决策 | 理由（以纯小白最友好为准） |
|---|---|---|
| D1 | 知识点总数定为 **58**（10 模块：6/7/7/5/6/6/6/5/6/4），位于 50~70 区间中部 | 每模块 4~7 点，既覆盖面试高频又不过载；数量固定使 P2 可断点续作 |
| D2 | 数据用**纯 JS 文件 + `<script>` 标签加载**（`window.SITE_DATA.registerModule`），不用 fetch/JSON | 免构建、`file://` 双击也能打开、GitHub Pages 零配置；JSON 无法内嵌函数（动画/交互） |
| D3 | 数据结构契约固化在 `docs/SCHEMA.md`，P1 骨架与 P2 内容都以它为准 | 子代理互不可见，必须靠文件级契约对齐 |
| D4 | 判题语义：`fill` 走归一化（去空白/小写/全角转半角）；`code` 用 1~5 个关键 token 忽略大小写子串匹配；`order` 下标排列深比较 | 静态站无后端，判题须确定性且对小白宽容；正则判题误伤率高故不用 |
| D5 | 间隔重复间隔表 `[0, 1, 3, 7, 14, 30]` 天；答对 box+1（封顶 5）、答错 box 归 0 | 艾宾浩斯曲线的简化 SM-2；规则可解释、可在页面向学员展示 |
| D6 | 动画约束：事件驱动 + CSS 过渡，禁 `setInterval` 轮询、禁外部库/URL | 防止 SPA 反复渲染导致定时器泄漏与控制台报错（DoD 要求零报错） |
| D7 | 知识图谱页用「模块分组 + 依赖链接 + 大纲序学习路径」实现，不做力导向图 | 大纲顺序已拓扑有序；力导向图对 58 点反而难读且实现风险高 |
| D8 | `MASTERED` 判定 = 该课全部例题至少答对一次；`box` 只受做题结果影响 | 用"做题"这一客观行为定义掌握，避免主观按钮误标 |
| D9 | 内容事实校准：子代理优先 WebSearch；不可用则文件首行标注 `// knowledge_source: "internal"` | 落实主提示词 §8 降级协议，可追溯 |
| D10 | 仓库不主动 `git init`（用户未要求提交），README 提供 init/push 全套命令 | 遵循"用户要求才做版本控制操作"；交付即可一键部署 |
| D11 | index.html 同时以 `<script>` 引入全部 10 个数据文件，P1 期先放空壳 stub（0 课） | 避免开发期 404 控制台报错；P2 直接覆写 stub 文件，天然支持中断续作 |
| D12 | 例题解析强制 ≥20 字且必须指出常见误区 | "即时判题 + 逐题解析"是模板硬性要素，校验器把"解析质量"变成可检查项 |
| D13 | 项目根加 `package.json`（`type: commonjs`, private） | 宿主机用户目录的 package.json 声明了 ESM，会让 Node 误判本项目 `.js`；声明 CommonJS 后校验器/单测可正常 `require`，对静态站本身零影响 |
| D14 | P2 子代理并发上限由 3 **降为 2** | 前两波"3 派 2 成"：每次第三路派发被平台限流（1302 错误）打断；原主提示词已预授权"环境并发宜更保守时改为 2"。降并发后重试成本更低 |
| D15 | M2 数据文件按"续写协议"恢复 | M2 子代理被限流打断时已完整写入 3/7 课并留 `/*__MORE__*/` 标记；重试时指示新子代理只续写剩余 4 课并闭合文件（§8 相同自包含提示词重试 1 次原则 + 幂等续作） |
| D16 | 复习队列只收「至少提交过一题」的课 | 纯浏览不产生记忆负担；否则一次快速翻阅就会让复习队列被 50+ 课淹没，对小白是负反馈。改 progress.js 的 dueForReview/stats 口径，与地图「今日复习」徽标一致 |
